# 公众号博主洞察台｜制作需求文档 v0.1

## 1. 产品目标

把微信关注的公众号从“名称列表”整理成可判断、可筛选、可复盘的博主画像：系统读取公众号发布内容，提取主题与表达特征，并结合可追溯的内容热度，为每个博主生成分类、标签、评分和代表文章。

## 2. 首版边界

首版先做网页端设置和调试，不直接依赖微信自动化。网页端支持演示数据、CSV/JSON 导入和后续采集器接入；分类结果必须展示依据，允许用户手动改类和加备注。微信桌面端读取、定时同步和安装包封装放到第二阶段。

## 3. 用户流程

1. 导入公众号清单或内容数据。
2. 设置分析时间范围、分类数量、热度权重和主题关键词。
3. 运行分析，得到总览、博主分类、热度排行和待复核项。
4. 点击博主查看内容样本、主题分布、热度趋势和系统判断依据。
5. 人工修改分类/标签、加入收藏或忽略，并保存设置。
6. 后续接入微信采集器后，增量同步新文章并重新计算。

## 4. 功能需求

### 4.1 数据接入

- 导入公众号名称、文章标题、发布时间、阅读/点赞/在看/评论等字段。
- 支持 CSV、JSON；字段缺失时明确标记“数据不足”，不伪造热度。
- 预留微信桌面端适配器接口：采集、清洗、去重、入库与分析分层。

### 4.2 内容分析

- 主题分类：AI/科技、商业/投资、产品/设计、职业/成长、文化/生活、新闻/资讯等。
- 统计主题占比、标题关键词、更新频率、内容长度与原创/转载（有字段时）。
- 每个分类结果提供至少 2 条证据：代表文章或高频主题词。

### 4.3 热度分析

- 默认热度指数：阅读 45% + 点赞 25% + 在看 20% + 评论 10%，均做同时间窗口内归一化。
- 内容热度与账号热度分开：账号热度为窗口内文章热度的中位数/稳定性综合值。
- 缺少互动字段时只计算“内容活跃度”，并显示口径提示。

### 4.4 运营与复核

- 博主状态：重点关注、观察中、低优先级、已忽略。
- 支持自定义分类、标签、备注和人工覆盖系统判断。
- 支持按分类、热度、更新频率、状态、关键词搜索和排序。
- 支持导出分析结果，未来支持桌面端本地数据库。

## 5. 页面结构

- 总览：账号总数、已分析内容、重点关注、待复核；分类分布与热度榜。
- 博主库：筛选栏 + 博主卡片/表格，展示分类、热度、更新频率、证据摘要。
- 博主详情：画像、主题占比、热度趋势、代表文章、系统依据、人工修正。
- 导入与设置：数据导入、时间窗口、评分权重、分类管理、同步入口。

## 6. 非功能需求

- 数据默认本地处理，微信登录态、聊天记录和个人信息不上传。
- 所有评分可解释、可复算；保留分析时间和数据范围。
- 先保证桌面宽屏操作，兼容 1024px 以上窗口；后续适配移动端。
- 分析失败时保留原始数据，并提示具体字段或采集器错误。

## 7. 阶段计划

- P0 网页原型：演示数据、总览、博主库、详情抽屉、导入设置、人工修正。
- P1 本地数据层：SQLite schema、CSV/JSON 导入、规则分析服务、结果持久化。
- P2 微信适配器：在用户本机微信桌面端读取明确授权范围内的公众号文章数据。
- P3 软件封装：桌面壳、定时同步、托盘入口、备份与升级。

## 8. 验收标准

1. 用户能在 2 分钟内看到一批公众号的分类与热度排序。
2. 任一分类结果都能回看到代表文章/主题词等证据。
3. 缺失热度字段时不会把估算值伪装成真实热度。
4. 用户能修改分类并在刷新后保持修改结果。
5. 数据采集器替换时不需要重写分析和网页展示层。

## 9. 逻辑自检结论

- “内容”和“热度”是两个独立维度，避免把高阅读误判成专业度。
- “分类”必须可解释且可人工覆盖，避免黑箱判断。
- 微信自动读取存在登录态、权限、版本兼容和平台规则风险，因此首版先用导入/演示数据验证产品逻辑。
- 仅靠公众号名称不能完成画像，最低有效数据是“账号 + 多篇文章 + 时间 + 热度字段（若有）”。
- 后续封装桌面软件时复用本地数据库和适配器，不把微信采集代码写进页面。
